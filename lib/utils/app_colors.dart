import 'package:flutter/material.dart';

// This file contains the color palette for the Pashudhan-AI app.
// Using a central color file makes it easy to manage and update the app's theme.

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF4CAF50); // A green shade for the main theme
  static const Color primaryDark = Color(0xFF388E3C);
  static const Color primaryLight = Color(0xFFC8E6C9);

  // Accent colors
  static const Color accent = Color(0xFFFF9800); // An orange shade for highlights
  static const Color accentDark = Color(0xFFF57C00);
  static const Color accentLight = Color(0xFFFFE0B2);

  // Text colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textOnPrimary = Colors.white;
  static const Color textOnAccent = Colors.white;

  // Background colors
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Colors.white;

  // Other colors
  static const Color divider = Color(0xFFBDBDBD);
  static const Color error = Color(0xFFD32F2F);
}
