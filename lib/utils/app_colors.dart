import 'package:flutter/material.dart';

// This file contains the color palette for the Pashudhan-AI app.
// Using a central color file makes it easy to manage and update the app's theme.

class AppColors {
  // Primary colors based on the new design
  static const Color primary = Color(0xFF5E8B3D);
  static const Color background = Color(0xFFF0F4EC);
  static const Color surface = Colors.white;

  // Text colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textOnPrimary = Colors.white;

  // Accent and other colors (can be used for other UI elements)
  static const Color accent = Color(0xFFFF9800);
  static const Color divider = Color(0xFFBDBDBD);
  static const Color error = Color(0xFFD32F2F);
  static const Color textOnAccent = Colors.white;
}
